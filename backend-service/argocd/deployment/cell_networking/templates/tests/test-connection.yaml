apiVersion: v1
kind: Pod
metadata:
  name: demo-app-test-connection
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: "{{ .Values.image.repository }}:{{ .Values.image.tag  }}"
      command: ['wget']
      args: ['demo-app-round-robbin-service:8080']
  restartPolicy: Never
