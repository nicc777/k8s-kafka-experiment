# Default values for demo-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: "ghcr.io/nicc777/container-python4aws"
  pullPolicy: IfNotPresent
  tag: "v3.12.3-3"

namespace: exp

baseDomain: example.tld

weights:
  target1: 50
  target2: 50

applicationNames:
  target1: front-end-ui-rest-v1
  target2: front-end-ui-rest-v1
