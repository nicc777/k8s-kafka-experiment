yamlApplicationConfig:
  kafka:
    clusters:
    - name: yaml
      bootstrapServers: kafka-0.kafka.confluent.svc.cluster.local:9092
  auth:
    type: disabled
  management:
    health:
      ldap:
        enabled: false
